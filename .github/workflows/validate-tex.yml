name: Validation fichiers LaTeX

on:
  push:
    branches:
      - '**'
  pull_request: # Ajouté pour permettre le commentaire PR
    branches:
      - '**' 
    paths:
      - 'src/**/*.tex'
      - '_preambules/**/*.tex'
      - 'tests/**'
      - '.github/workflows/validate-tex.yml'

jobs:
  validate:
    name: Tests de validation
    runs-on: ubuntu-latest
    
    # Permissions explicites pour écrire le commentaire
    permissions:
      contents: read
      pull-requests: write 

    container:
      image: texlive/texlive:latest

    steps:
      - name: Install Node needed for Actions
        # Les actions GitHub (checkout, github-script) ont besoin de Node.js.
        # L'image texlive ne l'a pas forcément. Le runner le monte souvent, 
        # mais il vaut mieux s'assurer que les deps système (curl, git) sont là.
        run: |
          apt-get update
          apt-get install -y git curl

      - name: Checkout
        uses: actions/checkout@v4

      # On utilise le Python du système (conteneur) plutôt que setup-python
      - name: Install Dependencies
        run: |
          apt-get install -y python3 python3-pip chktex
          # Installation des libs python au niveau user ou venv (option --break-system-packages nécessaire sur les Debian récents sans venv)
          pip3 install -r tests/requirements.txt --break-system-packages

      - name: Run validation tests
        run: |
          pytest tests/ -v --tb=short --maxfail=0
        continue-on-error: true

      - name: Generate summary report
        if: always()
        run: |
          python3 scripts/validate_exercises.py --report json > validation_report.json

      - name: Upload validation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: validation-report
          path: validation_report.json
          retention-days: 30

      - name: Comment PR with results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            try {
              const report = JSON.parse(fs.readFileSync('validation_report.json', 'utf8'));
              // Utilisation de context.repo au lieu de l'interpolation YAML pour éviter les erreurs de syntaxe JS
              const repoUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
              
              const body = `## 📋 Validation LaTeX

            ${report.summary}

            ${report.with_errors > 0 ? '❌ Des erreurs ont été détectées.' : '✅ Tous les tests passent !'}

            [Télécharger le rapport détaillé](${repoUrl})`;

              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: body
              });
            } catch (error) {
              console.error('Erreur lors de la création du commentaire:', error);
            }
            