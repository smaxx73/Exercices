copylines = 0; nblocks = 50; nlines = 50; if copylines == nil or nblocks == nil or nlines == nil then io.stderr:write("failed to parse arguments"); os.exit(2); end; yvaries = 'x'; yvaries = (yvaries == 'y'); infile = io.open("4R9m-tikz-3_tmp_contourtmp0.dat", "r"); outfile = io.open("4R9m-tikz-3_tmp_contourtmp0.table", "w"); mesh = PrepcMesh.new(yvaries, nblocks, nlines, copylines, infile, outfile); corners = false; levels = {-1,0,1,1.5,2}; for n = 1, #levels do level = tonumber(levels[n]); if level then mesh:contour(level, corners); end; end; io.close(infile); mesh:printcontours(); io.close(outfile);
