\begin{tikzpicture}[scale=1]
	\begin{axis}[
		title={Surface $A$},
		view={30}{20},
		colormap/viridis,
		xmin=-1, xmax=1,
		ymin=-1, ymax=1,
		zmin=0, zmax=2,
		xlabel=$x$,
		ylabel=$y$,
		zlabel={$f(x,y)$},
		axis equal,
		grid=major
		]
		\addplot3[
		domain=-1:1,
		domain y=-1:1,
		samples=40,
		samples y=40,
		surf,
		] {abs(x)+abs(y)};
	\end{axis}
\end{tikzpicture}