% Préambule commun pour toutes les figures TikZ standalone
\documentclass[tikz,border=2mm]{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{contourlua}

% Configuration des contours - SUPPRIMÉ car non supporté
% La sortie se fera dans le répertoire courant

% Autres packages si nécessaire
\usepackage{amsmath}
\usepackage{amssymb}

% Styles globaux (si besoin)
\tikzset{
	% Tes styles par défaut
}