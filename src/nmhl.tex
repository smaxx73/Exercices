\uuid{nmhl}
\exo7id{4545}
\auteur{quercia}
\organisation{exo7}
\datecreate{2010-03-14}
\isIndication{false}
\isCorrection{true}
\chapitre{Suite et série de fonctions}
\sousChapitre{Autre}

\contenu{
\texte{
Pour $n\in\N^*$ et $x\in[-1,1]$ on pose $u_n(x)=\frac{x^n\sin(nx)}n$.
}
\begin{enumerate}
    \item \question{Montrer que la série $\sum_{n=1}^\infty u_n(x)$ converge uniformément
    sur $[-1,1]$ vers une fonction continue, $f$.}
\reponse{Transformation d'Abel.}
    \item \question{Justifier la dérivabilité de~$f$ sur $]-1,1[$ et calculer $f'(x)$.
    En déduire $f(x)$.}
\reponse{$f(x) = \Arctan\left(\frac{x\sin x}{1-x\cos x}\right)$.}
    \item \question{En déduire la valeur de $\sum_{n=1}^\infty \frac{\sin n}n$.}
\reponse{$\frac{\pi-1}2$.}
\end{enumerate}
}
