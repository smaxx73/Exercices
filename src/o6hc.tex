\uuid{o6hc}
\exo7id{1755}
\auteur{maillot}
\organisation{exo7}
\datecreate{2001-09-01}
\isIndication{false}
\isCorrection{false}
\chapitre{Topologie}
\sousChapitre{Borne supérieure}

\contenu{
\texte{
On se propose de montrer que tout ouvert de $\R$ est une réunion
d'intervalles ouverts disjoints. On considère donc un ouvert
$U\subset\R$ et pour tout $x\in U$ on pose $$C(x)=\{y\in [x,+\infty[\ \mid
[x,y]\subset U\} \cup \{y\in ]-\infty,x[\ \mid [y,x]\subset U\}.$$
}
\begin{enumerate}
    \item \question{Montrer que $C(x)$ est un intervalle ouvert pour tout $x$.
(Considérer $\inf_{y\in C(x)}y$ et $\sup_{y\in C(x)}y$.)}
    \item \question{Pour tous $x,y$ dans $U$, montrer qu'on a $C(x)=C(y)$ ou
$C(x)\cap C(y)=\emptyset$.}
    \item \question{Conclure.}
\end{enumerate}
}
