\uuid{zeZd}
\exo7id{6638}
\auteur{queffelec}
\organisation{exo7}
\datecreate{2011-10-16}
\isIndication{false}
\isCorrection{false}
\chapitre{Fonction holomorphe}
\sousChapitre{Fonction holomorphe}

\contenu{
\texte{

}
\begin{enumerate}
    \item \question{Soit $f=P+iQ$ une fonction holomorphe dans un ouvert connexe non vide $\Omega$
de
$\Cc$. Montrer que les assertions suivantes sont équivalentes :
 
(i) $f$ est constante,\quad (ii)  $P$ est constante, \quad (iii)  $Q$ est
constante,\quad (iv)  $\bar f$ est holomorphe dans $U$, \quad (v)  $|f|$ est
constant.}
    \item \question{Soit $f,g\in H(U)$. On suppose que $g$ ne s'annule pas dans $U$ et
$f(z)\bar g(z)\in\Rr$ pour $z\in U$. Montrer qu'il existe $c\in\Rr$ telle
que $f=cg$.}
\end{enumerate}
}
