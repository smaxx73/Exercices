\uuid{nL7j}
\exo7id{4182}
\auteur{quercia}
\organisation{exo7}
\datecreate{2010-03-11}
\isIndication{false}
\isCorrection{true}
\chapitre{Fonction de plusieurs variables}
\sousChapitre{Dérivée partielle}

\contenu{
\texte{
On pose pour $(x,y)\in\R^2$~:
$$f(x,y) = x^2+y^2-2x^2y - \frac{4x^6y^2}{(x^4+y^2)^2}\quad\text{si }(x,y)\ne 0,
  \qquad f(0,0)=0.$$
}
\begin{enumerate}
    \item \question{Montrer que $f$ est continue sur $\R^2$.}
\reponse{$2x^4y^2 \le (x^4+y^2)^2$.}
    \item \question{Soit $\theta\in\R$ fixé et $g_\theta(r) = f(r\cos\theta,r\sin\theta)$.
Montrer que $g_\theta$ admet un minimum local strict en~$r=0$.}
\reponse{$g_\theta(r)\sim r^2$.}
    \item \question{Calculer $f(x,x^2)$. Conclusion~?}
\reponse{$f(x,x^2)=-x^4$. Donc $(0,0)$ n'est pas minimum local de~$f$.}
\end{enumerate}
}
