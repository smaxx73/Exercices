\uuid{l6Vh}
\exo7id{2892}
\auteur{quercia}
\organisation{exo7}
\datecreate{2010-03-08}
\isIndication{false}
\isCorrection{false}
\chapitre{Injection, surjection, bijection}
\sousChapitre{Application}

\contenu{
\texte{

}
\begin{enumerate}
    \item \question{Soit $f : F \to  E$ et $g : G  \to E$ deux applications.
    Montrer qu'il existe une
    application $h : G \to  F$ telle que $g = f\circ h$ si et seulement si :
    $g(G) \subset f(F)$.\\
    A quelle condition $h$ est-elle unique ?}
    \item \question{Soit $f: E \to F$ et $g : E \to G$ deux applications.
    Montrer qu'il existe une
    application $h: F\to G$ telle que $g = h\circ f$ si et seulement si :
    $\forall\ x,y \in E,\ \bigl( f(x) = f(y)  \Rightarrow  g(x) = g(y) \bigr)$.
    \\
    A quelle condition $h$ est-elle unique ?}
\end{enumerate}
}
