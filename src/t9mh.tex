\uuid{t9mh}
\exo7id{3453}
\auteur{quercia}
\organisation{exo7}
\datecreate{2010-03-10}
\isIndication{false}
\isCorrection{true}
\chapitre{Déterminant, système linéaire}
\sousChapitre{Calcul de déterminants}

\contenu{
\texte{

}
\begin{enumerate}
    \item \question{Soient ${a,b,c,d} : \R \to \R$ des fonctions dérivables et
    $f(x) = \begin{vmatrix} a(x) &b(x) \cr c(x) & d(x) \end{vmatrix}$.

    Montrer que $f$ est dérivable et que :
    $f'(x) = \begin{vmatrix} a'(x) &b(x)  \cr c'(x) &d(x)  \end{vmatrix}
           + \begin{vmatrix} a(x)  &b'(x) \cr c(x)  &d'(x) \end{vmatrix}$.}
    \item \question{Généraliser à un déterminant $n\times n$.}
    \item \question{Application :
    Calculer $\begin{vmatrix} 1 & \cos x          & \sin x          \cr
                        1 & \cos (x+\alpha) & \sin (x+\alpha) \cr
                        1 & \cos (x+\beta)  & \sin (x+\beta)  \cr\end{vmatrix}$.}
\reponse{
3.$\sin\alpha - \sin\beta - \sin(\alpha-\beta)$.
}
\end{enumerate}
}
