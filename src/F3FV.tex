\uuid{F3FV}
\exo7id{493}
\auteur{bodin}
\organisation{exo7}
\datecreate{1998-09-01}
\isIndication{false}
\isCorrection{false}
\chapitre{Propriétés de R}
\sousChapitre{Autre}

\contenu{
\texte{
Pout tout $x\in \Rr$ on note $E(x)$ sa partie enti\`ere et
$\{x\}$ sa partie d\'ecimale.
}
\begin{enumerate}
    \item \question{Tracer les graphes des fonctions
$x \mapsto E(x)$ et $x\mapsto \{x\}$.}
    \item \question{Montrer les relations suivantes :
$E(x)+E(y) \le E(x+y)$, $E(x+n)=E(x)+n$ pour tout $n\in \Zz$,
$E \left( \frac{E(nx)}{n} \right) = E(x)$ pour tout $n\in\Nn^*$.}
    \item \question{D\'eterminer $\lim E(x)$ et $\lim \{x\}$ lorsque
$x \rightarrow -1_+$ et $x \rightarrow -1_-$. Ces fonctions ont-elles
une limites lorsque $x \rightarrow -1$ ?}
\end{enumerate}
}
